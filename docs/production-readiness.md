# Production Readiness Report

> Generated by `pnpm release:rehearsal` (`scripts/production-readiness.ts`).
> Do not edit by hand — rerun the rehearsal to refresh it.

- **Generated:** 2026-07-30T04:24:48.038Z
- **Overall status:** PENDING HUMAN LAUNCH DEPENDENCIES
- **Checks:** 5 passed · 0 failed · 3 manual

## Checks

| Check                            | Status    | Evidence                                                                                                                                                                                   |
| -------------------------------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Seed safeguards                  | ✅ pass   | Seeding is rejected under APP_ENV=production and APP_ENV=staging; only local/test may seed.                                                                                                |
| Expand-contract migration safety | ✅ pass   | Every committed migration is labelled `-- migration-phase: expand`; contract steps must ship separately.                                                                                   |
| Full verification command        | ✅ pass   | `pnpm verify` runs format:check, lint, typecheck, test, build, lighthouse; `release:check` gates every release on it.                                                                      |
| Environment documentation        | ✅ pass   | Deployment checklist and .env.example document isolated OAuth, database, Inngest, VAPID, analytics, and secret configuration per environment.                                              |
| Preview isolation guard          | ✅ pass   | assertPreviewIsolation() rejects a Preview carrying APP_ENV=production, allows an isolated Preview, and runs at app boot (src/instrumentation.ts).                                         |
| Neon PITR retention (≥ 7 days)   | 🔶 manual | Provide NEON_API_KEY and NEON_PROJECT_ID and rerun; retention must be at least 7 days.                                                                                                     |
| Google OAuth verification        | 🔶 manual | Google OAuth consent-screen verification is a human launch dependency; production sign-in and Calendar access depend on it. Confirm the verified state per environment before launch.      |
| Restore drill (4-hour RTO)       | 🔶 manual | Run `pnpm restore:drill` before launch and after meaningful schema changes; record the recovery time in docs/runbooks/restore-drill.md. The initial recovery-time objective is four hours. |

## Remaining human launch dependencies

- **Neon PITR retention (≥ 7 days)** — Provide NEON_API_KEY and NEON_PROJECT_ID and rerun; retention must be at least 7 days.
- **Google OAuth verification** — Google OAuth consent-screen verification is a human launch dependency; production sign-in and Calendar access depend on it. Confirm the verified state per environment before launch.
- **Restore drill (4-hour RTO)** — Run `pnpm restore:drill` before launch and after meaningful schema changes; record the recovery time in docs/runbooks/restore-drill.md. The initial recovery-time objective is four hours.
