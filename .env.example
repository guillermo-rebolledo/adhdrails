# Use a separate Neon database for each environment.
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Required when running the seed command. Allowed values: local, test
APP_ENV=local
